@page
@model dudego.Pages.TbCustomer.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TbCustomer[0].DuiCustomer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TbCustomer[0].DrivingLic)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TbCustomer[0].NameCustomer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TbCustomer[0].PhoneNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TbCustomer[0].Address)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.TbCustomer) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DuiCustomer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DrivingLic)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NameCustomer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhoneNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdCustomer">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IdCustomer">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdCustomer">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
